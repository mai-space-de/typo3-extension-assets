services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    Maispace\MaiAssets\:
        resource: '../Classes/*'

    # -------------------------------------------------------------------------
    # Public services
    #
    # These services must be public so GeneralUtility::makeInstance() (used inside
    # static ViewHelper renderStatic() methods) can resolve them from the DI container.
    # -------------------------------------------------------------------------

    Maispace\MaiAssets\Cache\AssetCacheManager:
        public: true

    Maispace\MaiAssets\Service\AssetProcessingService:
        public: true

    Maispace\MaiAssets\Service\ScssCompilerService:
        public: true

    # SpriteIconRegistry implements SingletonInterface.
    # The DI container respects SingletonInterface and returns the same instance
    # throughout the request when resolved via GeneralUtility::makeInstance().
    # Must be public so SvgSpriteMiddleware (resolved via DI) and SvgSpriteViewHelper
    # (resolved via GeneralUtility::makeInstance in static renderStatic) both get
    # the identical singleton with the already-built sprite.
    Maispace\MaiAssets\Registry\SpriteIconRegistry:
        public: true

    # FontRegistry implements SingletonInterface — same reasoning as SpriteIconRegistry.
    # Must be public so FontPreloadEventListener (DI-resolved) always gets the same
    # singleton instance that has already performed auto-discovery.
    Maispace\MaiAssets\Registry\FontRegistry:
        public: true

    # ImageRenderingService must be public so the ViewHelpers (which use static
    # renderStatic() and resolve dependencies via GeneralUtility::makeInstance())
    # receive the DI-configured instance with ImageService and LoggerInterface injected.
    Maispace\MaiAssets\Service\ImageRenderingService:
        public: true

    # WarmupCommand — auto-discovered via #[AsCommand] attribute + autoconfigure: true.
    # Run at deploy time: php vendor/bin/typo3 maispace:assets:warmup

    # FontPreloadEventListener — registered as a real event listener (not an example).
    # Font preloading is a core feature of maispace_assets, not an opt-in hook.
    Maispace\MaiAssets\EventListener\FontPreloadEventListener:
        tags:
            -   name: event.listener
                identifier: 'maispace-assets-font-preload'
                event: TYPO3\CMS\Core\Page\Event\BeforeStylesheetsRenderingEvent

    # -------------------------------------------------------------------------
    # Example event listeners
    #
    # These listeners are intentionally commented out. They ship as working
    # examples to demonstrate how to hook into the maispace_assets event system.
    #
    # To activate a listener, copy the relevant block into your site package's
    # Configuration/Services.yaml and uncomment it there. Do NOT uncomment here,
    # as that would activate the example for every project using this extension.
    #
    # Available events:
    #   - Maispace\MaiAssets\Event\AfterCssProcessedEvent
    #   - Maispace\MaiAssets\Event\AfterJsProcessedEvent
    #   - Maispace\MaiAssets\Event\AfterScssCompiledEvent
    #   - Maispace\MaiAssets\Event\BeforeSpriteSymbolRegisteredEvent
    #   - Maispace\MaiAssets\Event\AfterSpriteBuiltEvent
    #   - Maispace\MaiAssets\Event\BeforeImageProcessingEvent
    #   - Maispace\MaiAssets\Event\AfterImageProcessedEvent
    # -------------------------------------------------------------------------

    # Maispace\MaiAssets\EventListener\AfterCssProcessedEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-css'
    #             event: Maispace\MaiAssets\Event\AfterCssProcessedEvent

    # Maispace\MaiAssets\EventListener\AfterJsProcessedEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-js'
    #             event: Maispace\MaiAssets\Event\AfterJsProcessedEvent

    # Maispace\MaiAssets\EventListener\AfterScssCompiledEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-scss'
    #             event: Maispace\MaiAssets\Event\AfterScssCompiledEvent

    # Maispace\MaiAssets\EventListener\BeforeSpriteSymbolRegisteredEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-before-sprite-symbol'
    #             event: Maispace\MaiAssets\Event\BeforeSpriteSymbolRegisteredEvent

    # Maispace\MaiAssets\EventListener\AfterSpriteBuiltEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-after-sprite-built'
    #             event: Maispace\MaiAssets\Event\AfterSpriteBuiltEvent

    # Maispace\MaiAssets\EventListener\BeforeImageProcessingEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-before-image-processing'
    #             event: Maispace\MaiAssets\Event\BeforeImageProcessingEvent

    # Maispace\MaiAssets\EventListener\AfterImageProcessedEventListener:
    #     tags:
    #         -   name: event.listener
    #             identifier: 'maispace-assets-example-after-image-processed'
    #             event: Maispace\MaiAssets\Event\AfterImageProcessedEvent
